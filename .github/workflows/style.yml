name: Style Clojure
on: [pull_request]
jobs:
  cljstyle:
    name:  Workflow
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3.0.2
      - name: cljstyle
        uses: practicalli/clojure-style-action@1
        with:
          github_token: ${{ secrets.github_token }}
          reporter: github-pr-review
